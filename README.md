# C-Program-to-Display-Student-Details-Using-Registration-Number
This C++ program asks the user to enter a registration number. It checks the number using if–else conditions. If the number matches the stored value, the program displays the college name, student name, and marks. If the number does not match, it prints False.
